# Python
__pycache__/
*.pyc
*.egg-info/
/build/
/dist/

# Local env & data
.env
*.sqlite3
.venv/
.pytest_cache/
.DS_Store
# collectstatic output (STATIC_ROOT). Root-anchored: this is the one directory
# at the repo root, and an unanchored rule would swallow a real source dir of
# that name later. Production collects inside the Docker build, never here.
/staticfiles/

# Local dev tooling — NOT published
.claude/
.serena/
.superpowers/
# Review packages and other per-slice working files written by the tuckit
# skills. Local-only: a stray `git add -A` would publish them.
.tuckit/

# Brainstorming specs and implementation plans — local-only, never publish
docs/superpowers/
